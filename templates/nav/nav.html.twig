<div class="navbar-fixed ">
    <nav>
        <div class="nav-wrapper">
            <a href="#" data-target="mobile-demo" class="sidenav-trigger right"><i class="material-icons">menu</i></a>
            <a href="{{ path('index') }}" class="brand-logo left">Selon une étude</a>
            <ul id="nav-mobile" class="hide-on-med-and-down">
                <li class="right">
                    <a href="https://twitter.com/selonuneetudefr" target="_blank">
                        <img src="{{ asset('images/icones/Twitter_Social_Icon_Rounded_Square_Color.png') }}"
                             alt="icone
                twitter" title="Suivez-nous sur Twitter">
                    </a>
                </li>
                <li class="right">
                    <a href="https://www.facebook.com/Selon-une-%C3%A9tude-1707896749331928/?modal=admin_todo_tour" target="_blank">
                        <img src="{{ asset('images/icones/FB-fLogo-Blue-broadcast-2.png') }}" alt="icone facebook"
                             title="Suivez-nous sur Facebook">
                    </a>
                </li>
                <li class="nav-follow right">Suivez-nous : </li>
                {% if is_granted('ROLE_USER') %}
                    <li class="nav-list-links">
                        <a class="dropdown-button right dropdown-trigger" href="#!" data-target="dropdown2"><i class="material-icons right">arrow_drop_down</i>
                            Bonjour <strong>{{ app.user.username }} </strong>
                        </a>
                    </li>
                {% else %}
                    <li class="nav-list-links">
                        <a class="dropdown-button right dropdown-trigger" href="#!" data-target="dropdown1">
                            <i class="material-icons right">arrow_drop_down</i>
                            Inscription / Connexion
                        </a>
                    </li>
                {% endif %}
                <li><a href="{{ path('all_actualites') }}">Actualités</a></li>
                <li><a href="{{ path('proposer_etude') }}">Proposer une étude</a></li>
                <li><a href="{{ path('contact') }}">Nous contacter</a></li>
            </ul>
        </div>
    </nav>
</div>

<!-- DROPDOWN -->
<ul id="dropdown1" class="dropdown-content">
    <li><a href="{{ path('user_registration') }}" class="white-text">Inscription</a></li>
    <li class="divider"></li>
    <li><a href="{{ path('login') }}" class="white-text">Connexion</a></li>
</ul>
{% if is_granted('ROLE_USER') %}
    <ul id="dropdown2" class="dropdown-content">
        <li><a href="{{ path('proposer_etude') }}" class="white-text">Proposer une étude</a></li>
        <li class="divider"></li>
        <li><a href="{{ path('view_profile', {'username': app.user.username}) }}" class="white-text">Mon compte</a></li>
        <li class="divider"></li>
        <li><a href="{{ path('security_logout') }}" class="white-text">Déconnexion</a></li>
        {% if is_granted('ROLE_ADMIN') %}
            <li class="divider"></li>
            <li><a href="{{ path('admin_index') }}" class="white-text">Admin</a></li>
        {% endif %}
    </ul>
{% endif %}

<!-- MOBILE -->
<ul class="sidenav" id="mobile-demo">
    <li><a href="{{ path('proposer_etude') }}">Proposer une étude</a></li>
    <li><div class="divider"></div></li>
    {% if is_granted('ROLE_USER') %}
    <li class="no-padding">
        <ul class="collapsible collapsible-accordion">
            <li>
            <a class="collapsible-header" href="#!"><i class="material-icons right">arrow_drop_down</i>
                Bonjour <strong>{{ app.user.username }} </strong>
            </a>
                <div class="collapsible-body">
                    <ul>
                        <li><a href="{{ path('view_profile', {'username': app.user.username}) }}" class="white-text">Mon compte</a></li>
                        <li class="divider"></li>
                        <li><a href="{{ path('security_logout') }}">Déconnexion</a></li>
                        {% if is_granted('ROLE_ADMIN') %}
                        <li class="divider"></li>
                        <li><a href="{{ path('admin_index') }}">Admin</a></li>
                        {% endif %}
                    </ul>
                </div>
            </li>
        </ul>
        </li>
    {% else %}
    <li class="no-padding">
        <ul class="collapsible collapsible-accordion">
        <li>
            <a class="collapsible-header">
                <i class="material-icons right">arrow_drop_down</i>
                Inscription / Connexion
            </a>
                <div class="collapsible-body">
                    <ul>
                        <li>
                            <a href="{{ path('user_registration') }}">Inscription</a>
                        </li>
                        <li class="divider"></li>
                        <li><a href="{{ path('login') }}">Connexion</a></li>
                    </ul>
                </div>
        </li>
        </ul>
    </li>
    {% endif %}
    <li><div class="divider"></div></li>
    <li><a href="{{ path('all_actualites') }}">Actualités</a></li>
    <li><div class="divider"></div></li>
    <li><a href="{{ path('contact') }}">Nous contacter</a></li>
</ul>
