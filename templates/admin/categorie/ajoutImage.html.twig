{% extends 'base.html.twig' %}
{% block body %}
    {% include 'admin/nav/navAdmin.html.twig' %}
    <section class="row admin-form-ajout z-depth-2">
        <article class="col s12 m8 offset-m2">
            <div>
                <h1 class="center">Ajouter une image</h1>
            </div>
            {{ form_start(form) }}
            <div>
                {{ form_label(form.image) }}
                {{ form_widget(form.image) }}
                {{ form_errors(form.image) }}
            </div>
            <div class="input-field">
                {{ form_label(form.imageName, 'Nom de l\'image') }}
                {{ form_widget(form.imageName) }}
                {{ form_errors(form.imageName) }}
            </div>
            <div class="center">
                {{ form_widget(form.ajouter) }}
            </div>
            {{ form_end(form) }}
        </article>
    </section>

{% endblock %}
